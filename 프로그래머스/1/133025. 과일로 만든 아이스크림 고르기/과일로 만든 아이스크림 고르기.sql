-- 코드를 입력하세요
SELECT info.FLAVOR FROM FIRST_HALF as half INNER JOIN ICECREAM_INFO as info ON half.FLAVOR = info.FLAVOR WHERE TOTAL_ORDER > 3000 and INGREDIENT_TYPE  = 'fruit_based' ORDER BY TOTAL_ORDER DESC;